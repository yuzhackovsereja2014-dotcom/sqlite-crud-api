{
  "name": "sqlite-crud-api-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free JSON CRUD API backed by SQLite",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  }
}
